<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="DRPS_PHCA.WebWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Custom title bar with icon and title text -->
        <Grid x:Name="TitleBarDragArea" Grid.Row="0" Height="32" Background="Transparent">
            <StackPanel Orientation="Horizontal" Spacing="8"
                        VerticalAlignment="Center" Margin="12,0,0,0">
                <Image Source="ms-appx:///Assets/Square44x44Logo.scale-100.png"
                       Width="16" Height="16" VerticalAlignment="Center" />
                <TextBlock x:Name="TitleText"
                           VerticalAlignment="Center"
                           Style="{ThemeResource CaptionTextBlockStyle}" />
            </StackPanel>
        </Grid>

        <!-- WebView2 -->
        <WebView2 x:Name="ContentWebView" Grid.Row="1" />

        <!-- Loading overlay -->
        <Grid x:Name="LoadingOverlay" Grid.Row="1"
              Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="12">
                <ProgressRing IsActive="True" Width="32" Height="32" />
                <TextBlock Text="Loading..."
                           Style="{ThemeResource BodyTextBlockStyle}"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           HorizontalAlignment="Center" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
